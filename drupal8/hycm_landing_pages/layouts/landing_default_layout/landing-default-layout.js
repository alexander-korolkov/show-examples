/**
 * @file
 * Custom behaviors for landing_default_layout layout.
 */

(function (Drupal) {

  'use strict';

  Drupal.behaviors.landingDefaultLayout = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
